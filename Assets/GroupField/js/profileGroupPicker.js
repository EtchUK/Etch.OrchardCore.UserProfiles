window.initializeProfileGroupPicker = function(
    elementId,
    selectedItems,
    tenantPath,
    multiple
  ) {
    window.initializeProfilePicker(elementId, selectedItems, tenantPath, multiple, 'ProfileGroupPicker');
  };
  