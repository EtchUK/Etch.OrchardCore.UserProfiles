@model Etch.OrchardCore.UserProfiles.Subscriptions.ViewModels.EditSubscriptionLevelPartSettingsViewModel

<div class="form-group">
    <div class="custom-control custom-checkbox">
        <input type="checkbox" class="custom-control-input" asp-for="Multiple" checked="@Model.Multiple">
        <label class="custom-control-label" asp-for="Multiple">@T["Allow Multiple"]</label>
        <span class="hint">@T["Whether to allow multiple subscriptions to be selected"]</span>
    </div>
</div>
